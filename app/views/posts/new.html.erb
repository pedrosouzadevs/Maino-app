<%= turbo_frame_tag "modal" do %>
  <div>
    <h2>Add a post</h2>
    <%= simple_form_for Post.new do |f| %>
      <%= f.input :title, label: "Write your post title here:" %>
      <%= f.input :content, label: "Write your post content here:" %>
      <%= f.submit class: "button-50" %>
    <% end %>
    <%= link_to "Cancel", "#", data: {
      controller: "modal",
      action: "modal#close"
    }, class: "cancel-button" %>
  </div>
<% end %>
